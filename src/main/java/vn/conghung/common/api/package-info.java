/**
 * API response envelope types (records) shared across services.
 *
 * <p>Null-marked (JSpecify): all types are non-null by default; nullable members are
 * explicitly annotated {@link org.jspecify.annotations.Nullable} (TS-018 gap G8).
 */
@NullMarked
package vn.conghung.common.api;

import org.jspecify.annotations.NullMarked;
